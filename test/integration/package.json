{
    "name": "integration-test",
    "packageManager": "yarn@4.7.0",
    "scripts": {
        "node-build": "tsc && NODE_OPTIONS='--import tsx' qrill build",
        "node-dev": "tsc --watch & NODE_OPTIONS='--import tsx' qrill dev",
        "build": "tsc && qrill build",
        "dev": "tsc --watch & qrill dev",
        "check": "biome check --write ./site"
    },
    "dependencies": {
        "@biomejs/biome": "^1.9.4",
        "qrill": "../../qrill-0.1.0-alpha.8.tgz",
        "typescript": "^5.9.2"
    }
}
